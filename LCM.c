#include<stdio.h>
int main()
{
    int x,y,max;
    printf("Enter two numbers to find their LCM");
    scanf("%d %d ",x,y);
    if (x>y)
    x=y;
    else
    x=x;
    for (int i = 0; i < x && i < y ; i++)
    {
        /* code */
    }
    
    
}